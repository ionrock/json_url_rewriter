============
Installation
============

At the command line::

    $ easy_install json_url_rewriter

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv json_url_rewriter
    $ pip install json_url_rewriter
